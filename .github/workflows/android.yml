name: Compila APK Android

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4

      - name: Configurazione Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Installazione Dipendenze Web
        run: npm ci || npm install

      - name: Build dell'applicazione Web
        run: npm run build

      - name: Configurazione Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Configurazione Android SDK
        uses: android-actions/setup-android@v3

      - name: Sincronizzazione Capacitor
        run: npx cap sync android

      - name: Compilazione APK e AAB con Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug bundleRelease

      - name: Caricamento APK come Artefatto del Run
        uses: actions/upload-artifact@v4
        with:
          name: FitoVeneto-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Caricamento AAB come Artefatto del Run
        uses: actions/upload-artifact@v4
        with:
          name: FitoVeneto-AAB
          path: android/app/build/outputs/bundle/release/app-release-unsigned.aab

      - name: Creazione Release su GitHub ed Upload APK e AAB
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="v${{ github.run_number }}"
          echo "Creazione release $TAG_NAME..."
          gh release create "$TAG_NAME" \
            android/app/build/outputs/apk/debug/app-debug.apk \
            android/app/build/outputs/bundle/release/app-release-unsigned.aab \
            --title "FitoVeneto App - Versione $TAG_NAME" \
            --notes "Scarica l'APK per il test diretto o il file .aab (Bundle) per il caricamento sul Google Play Store." \
            --target "${{ github.ref_name }}"
